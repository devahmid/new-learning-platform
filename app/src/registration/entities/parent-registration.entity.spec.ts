import { ParentRegistrationEntity } from './parent-registration.entity';

describe('ParentRegistrationEntity', () => {
  it('should be defined', () => {
    expect(new ParentRegistrationEntity()).toBeDefined();
  });
});
