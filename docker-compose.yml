# services:
#   nginx:
#     build:
#       context: ./frontend
#       dockerfile: Dockerfile
#     container_name: nginx
#     ports:
#       - "80:80"
#       - "443:443"
#     volumes:
#       - ./certbot/www:/var/www/certbot
#       - ./certbot/conf:/etc/letsencrypt
#       - ./nginx/conf.d:/etc/nginx/conf.d
#     depends_on:
#       - nest
#     restart: always

#   nest:
#     build: ./app
#     container_name: nest
#     restart: always
#     depends_on:
#       - postgres
#     env_file:
#       - .env
#     volumes:
#       - ./app/uploads:/app/uploads
#     # ❌ NO ports: exposed — handled by nginx reverse proxy

#   postgres:
#     image: postgres:15
#     container_name: postgres
#     restart: always
#     environment:
#       POSTGRES_DB: ${POSTGRES_DB}
#       POSTGRES_USER: ${POSTGRES_USER}
#       POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
#     volumes:
#       - pgdata:/var/lib/postgresql/data

#   certbot:
#     image: certbot/certbot
#     container_name: certbot
#     volumes:
#       - ./certbot/www:/var/www/certbot
#       - ./certbot/conf:/etc/letsencrypt
#     entrypoint: ""
#     command: sh -c "echo 'Certbot ready'"

#   portainer:
#     image: portainer/portainer-ce
#     container_name: portainer
#     restart: always
#     environment:
#       - ADMIN_USERNAME=${PORTAINER_USER}
#       - ADMIN_PASSWORD=${PORTAINER_PASSWORD}
#     volumes:
#       - /var/run/docker.sock:/var/run/docker.sock
#       - portainer_data:/data
#     # ❌ pas besoin d'exposer 9000 — nginx reverse proxy s'en charge

#   grafana:
#     image: grafana/grafana
#     container_name: grafana
#     restart: always
#     environment:
#       - GF_SECURITY_ADMIN_USER=${GRAFANA_USER}
#       - GF_SECURITY_ADMIN_PASSWORD=${GRAFANA_PASSWORD}
#     volumes:
#       - grafana_data:/var/lib/grafana
#     # ❌ pas besoin d'exposer 3001 — nginx reverse proxy s'en charge

#   loki:
#     image: grafana/loki:2.9.0
#     container_name: loki
#     command: -config.file=/etc/loki/local-config.yaml
#     restart: always
#     ports:
#       - "3100:3100"

#   promtail:
#     image: grafana/promtail:2.9.0
#     container_name: promtail
#     volumes:
#       - /var/log:/var/log
#       - /etc/machine-id:/etc/machine-id:ro
#       - /var/lib/docker/containers:/var/lib/docker/containers:ro
#       - /var/run/docker.sock:/var/run/docker.sock
#       - ./promtail-config.yaml:/etc/promtail/config.yaml
#     command: -config.file=/etc/promtail/config.yaml
#     restart: always

# volumes:
#   pgdata:
#   portainer_data:
#   grafana_data:

version: "3.8"

services:
  backend:
    build:
      context: ./app
      dockerfile: Dockerfile
    container_name: backend
    env_file:
      - .env
    volumes:
      - ./app:/app
    depends_on:
      - db

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: frontend
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./certbot/conf:/etc/letsencrypt
      - ./certbot/www:/var/www/certbot
      - ./frontend/nginx/default.conf:/etc/nginx/conf.d/default.conf
    depends_on:
      - backend

  db:
    image: postgres:15-alpine
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: admin
      POSTGRES_DB: edu_db
    ports:
      - "5433:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

  grafana:
    image: grafana/grafana
    restart: always
    ports:
      - "3001:3000"

  portainer:
    image: portainer/portainer-ce
    container_name: portainer
    restart: always
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - portainer_data:/data

volumes:
  postgres_data:
  portainer_data:
